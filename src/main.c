#include <tickernelEngine.h>
int main()
{
    // struct timespec timespec;
    // timespec_get(&timespec, TIME_UTC);
    // char buff[100];

    // strftime(buff, sizeof buff, "%D %T", gmtime(&timespec.tv_sec));
    // printf("Current time: %s.%09ld UTC\n", buff, timespec.tv_nsec);
    // printf("Raw timespec.tv_sec: %jd\n", timespec.tv_sec);
    // printf("Raw timespec.tv_nsec: %09ld\n", timespec.tv_nsec);
    printf("Hello Tickernel!");
}